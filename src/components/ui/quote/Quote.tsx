export interface QuoteProps {
  quoteText: string;
  userImage: string;
  userName: string;
  userPosition: string;
}

export default function Quote({
  quoteText,
  userImage,
  userName,
  userPosition,
}: QuoteProps) {
  return <div></div>;
}
