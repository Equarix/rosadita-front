export interface NextArticleProps {
  title: string;
  header: string;
  buttonText: string;
  articleUrl: string;
}

export default function NextArticle({
  title,
  header,
  buttonText,
  articleUrl,
}: NextArticleProps) {
  return <div></div>;
}
